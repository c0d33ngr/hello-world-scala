package foo

object Example {
  def main(args: Array[String]): Unit = {
    println(hello())
  }

  def hello(): String = {
    "Hello, world!" // This function returns a greeting message.    
  }
}
// This is a simple Scala program that prints "Hello, world!" to the console.
// It serves as an example of a basic Scala application structure.
// The main method is the entry point of the application.